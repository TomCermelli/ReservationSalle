{% extends 'base.html.twig' %}

{% block body %}
    <h1>Semaine</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ semaine.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ semaine.jour }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('semaine_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('semaine_edit', { 'id': semaine.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
